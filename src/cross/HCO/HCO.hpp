#ifndef HCO_HPP
#define HCO_HPP

#include <vector>
using std::vector;

namespace HCO{
    vector<int> cross(vector<int>,vector<int>);

    bool isNeighbor(const vector<int>,const int,const int);
};

#endif