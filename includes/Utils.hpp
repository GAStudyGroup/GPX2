#ifndef UTILS_HPP
#define UTILS_HPP

#include <utility> 
#include <cmath>
#include <string>

int distance(int,int);
int distance(std::string,std::string);

int EUC_2D(int,int);
int GEO(int,int);
int ATT(int,int);
#endif